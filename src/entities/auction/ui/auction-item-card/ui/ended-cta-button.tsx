import Button from "@/shared/ui/button/button";

export default function EndedCtaButton() {
  return (
    <Button size="lg" className="w-full font-semibold" disabled aria-label="경매 종료">
      경매 종료
    </Button>
  );
}
